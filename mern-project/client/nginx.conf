server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # React Router - SPA support
        try_files $uri /index.html;
    }

    location /health {
        return 200 "healthy";
    }
}
